list_of_dependencies <- function(){
  "The following list of packages are required for the proper functioning of this package:
  general packages for proper functioning of the package: dplyr, tidyr, lubridate, sf, readxl, openxlsx, purrr

  packages necessary for spatial functions to work (yaps, glatos, Vtrack): plotKML, rgeos, rgdal, sf, sp, geodist, gdistance, geosphere, jsonlite, raster
  rgeos, rgdal, sf, sp, geodist, gdistance, geosphere, jsonlite, raster
  packages necessary for yaps functions to work: yaps, TMB, splusTimeDate, splusTimeSeries
  packages necessary for Vtrack functions to work: maptools
  "
}


